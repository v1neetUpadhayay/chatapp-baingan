# github
# start git
git init

git status
git remote add origin git@github.com:v1neetUpadhayay/chatapp-baingan.git
# to check which repo/server are we connected to
git remote -v

# in main direntory and adds everything
git branch
git check
git add .
git commit -m "message"
git push origin

git pull origin master


## activate env
.\venv_name\Scripts\activate

## start django app
python manage.py runserver


# creating a new env
python -m venv baingan_env

# activate env
python -m venv baingan_env

# to check what packages are already in env 
pip freeze

# setup django
pip install django
django-admin --version

# start a new application
django-admin startproject PROJECTNAME


  